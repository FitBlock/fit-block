{
  "name": "fit-block",
  "version": "1.0.0",
  "scripts": {
    "build":"npx lerna run build",
    "bootstrap": "npx lerna bootstrap",
    "bootstrapc": "npx lerna bootstrap --registry=https://registry.npm.taobao.org",
    "installc": "npm install --registry=https://registry.npm.taobao.org",
    "list": "npx lerna list",
    "link": "npx lerna link",
    "test": "npm run build && npx lerna run test"
  },
  "keywords": [
    "fit",
    "block",
    "fit-block",
    "bitcoin",
    "btc"
  ],
  "devDependencies": {
    "lerna": "^3.20.1",
    "@types/node": "^12.12.14",
    "typescript": "^3.7.2"
  }
}
